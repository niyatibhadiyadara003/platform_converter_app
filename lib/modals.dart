class User
{
  int id;
  String image;
  String title;
  String subtitle;
  String time;

  User({required this.image,required this.subtitle,required this.title,required this.time,required this.id});
}